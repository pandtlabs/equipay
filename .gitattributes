# Mark vendored third-party bundles + dev docs so GitHub language stats
# don't treat them as equiPay's own code.
vendor/**            linguist-vendored
docs/**              linguist-documentation

# Keep icon PNGs as binary; some tools otherwise try to diff them as text.
icons/*.png          binary
